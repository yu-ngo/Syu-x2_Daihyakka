<!--ここでは自身が投稿を行います。-->
<h1>大百科の一節</h1>
<%= form_with model: @post do |f| %>
  <h4>題名</h4>
  <%= f.text_field :title %>
  <h4>読み方</h4>
  <%= f.text_field :read %>
  <h4>意味</h4>
  <%= f.text_field :meaning %>
  <%= f.submit '作成' %>
<% end %>