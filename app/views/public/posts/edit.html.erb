<!--ここでは投稿の一時保存を行い、編集ができます。-->
<%= form_with model: @post do |f| %>
  <h4>題名</h4>
  <%= f.text_field :title %>
  <h4>読み方</h4>
  <%= f.text_field :read %>
  <h4>意味</h4>
  <%= f.text_field :meaning %>
  <%= f.submit '更新' %>
<% end %>